import { LocationByDistancePipe } from './location-by-distance.pipe';

describe('LocationByDistancePipe', () => {
  it('create an instance', () => {
    const pipe = new LocationByDistancePipe();
    expect(pipe).toBeTruthy();
  });
});
